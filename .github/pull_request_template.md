## Summary

<!-- Explain what changed and why. -->

## Validation

- [ ] `npm run verify`
- [ ] Tests added or updated where behavior changed

## Release

Every merged pull request creates a release. Apply at most one label:

- No label or `release:patch` for a patch release
- `release:minor` for a minor release
- `release:major` for a major release
